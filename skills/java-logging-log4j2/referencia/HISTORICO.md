# Histórico — java-logging-log4j2

Registro de rodadas de evolução da skill (movido do corpo do SKILL.md para progressive disclosure; conteúdo verbatim).

- **2026-07-19 — Rodada 2 (destravamento, decisões D-A..D-D do Jeremias):** defeito forte do Painel 2 corrigido — o invariante "placeholder `{}`, nunca concatenação" era estruturalmente incompatível com a fachada SIGO `Log.erro(String, Throwable)` (sem slot de substituição); reescrito para a forma honesta: placeholder onde a API dá o slot (Logger direto), formatação antes da chamada onde a fachada só aceita `String` pronta **é o padrão da casa** — o invariante real é nunca concatenar dado sensível e nunca perder o `Throwable`, não a forma sintática da chamada. Eval caso 1 ganhou assertion expondo essa forma. Guardrails deduplicados por referência cruzada: "não silenciar exceção" e "não logar segredo" (repetiam RO-08 quase literal) e "não abrir `LogManager` ao lado da fachada" (repetia "O que VARIA") viraram um ponteiro único às seções-fonte, com a nuance nova de que "só para esse log específico" é o início de duas formas convivendo no projeto. Caso 2 do eval (logger-por-classe) verificado contra as referências reais do Gradup (`springboot-repository-service`, `springboot-entity`, `springboot-controller-thymeleaf`) — nenhuma cita prática de logging (Gradup é Spring/SLF4J-shaped, não há Log4j2 real documentado); caso 2 mantido `origem: sintetico` com nota declarada, não forçado a "real" (RO-01). Todos os casos do `evals.json` ganharam `origem` (`real`|`sintetico`, com fonte quando `real`), `data` e `sessao`.
- **2026-07-19 — Correção pós-Painel 1 (convergências dos 3 juízes):** citação de regra corrigida — declarar `SUPOSICAO:` no greenfield é **RO-01** ("nada de chute de API"), não RI-01; erro corrigido nas 3 pontas (SKILL.md, referência, evals.json caso 3) que antes divergiam entre si. "Gabarito SIGO" saiu de dentro de "O que VARIA por projeto" (onde duplicava quase 1:1 a referência) e virou ponteiro mínimo numa seção própria `## Referências/Few-shots` (anatomia §5b) — valores concretos (fachada, logger `"COT"`, rotação/retenção) vivem só em `referencia-exemplos-reais-sigo.md`, fonte única. Fluxo item 4 ganhou critério checável (grep senha/token/credencial/hash). Referência: item 5 das Convenções REAIS cortado ao que o trecho verbatim ali mesmo comprova (Console/Root não tinham verbatim de apoio — RO-01, não inventar). Pendência não corrigida nesta rodada: `evals/placar-baseline.md` (fora do escopo de arquivos editáveis desta correção).
- **2026-07-19 — Evolução ao 9,5 (espelhar-não-impor, núcleo SIGO):** corpo reestruturado em **invariantes inegociáveis** (RO-08: nunca printStackTrace/System.out/System.err, Throwable último argumento, nunca dado sensível, nunca engolir exceção, placeholder) × **o que varia por projeto** (logger por classe × fachada única, nome do logger, nomenclatura dos métodos, rotação/retenção — espelhar, não prescrever). Resolvida a fricção "corpo mandava `Logger` por classe × referência SIGO usa fachada única `Log` com logger nomeado `"COT"`" — greenfield agora cai no default genérico marcado `SUPOSICAO:` (RO-01). Verificação de fechamento (RI-04) diferenciada: grep/build sempre executável × rotação/retenção como SKIP declarado (nem o SIGO prova isso em sessão). Eval em `evals/evals.json` (3 casos: SIGO → fachada única; outro projeto com logger-por-classe → espelha o outro estilo mantendo os invariantes; greenfield → default genérico com `SUPOSICAO:` declarada).
- **2026-07-18 — Few-shot de código real (2ª leva Java, código do SIGO):** criada `referencia-exemplos-reais-sigo.md` (fonte: código vivo do SIGO/sistema-cot). Baseline §11: sem o gabarito o gerador diverge nos pontos-chave da casa (ver referência). Degrau §6.10: 2 (referência nova). Notas em `rodadas/onda-fewshots-2026-07-18-notas.md`.
