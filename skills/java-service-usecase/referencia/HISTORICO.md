# Histórico — java-service-usecase

Registro de rodadas de evolução da skill (movido do corpo do SKILL.md para progressive disclosure; conteúdo verbatim).

- **2026-07-19 — Correção FINAL pós-Painel 3 (prova de acionamento sem fonte rastreável):** `evals/placar-baseline.md` não tinha a coluna `acionou S/N` exigida por `PADRAO-DE-AUTORIA.md` §11.5 (só "Aderiu") e a nota de rodapé citava "eval de frases do GUIA" como fonte do acionamento — mas `GUIA-DE-CHAMADAS-SKILLS.md` é uma tabela estática de frases-gatilho, não um log de execução com veredito S/N por caso (RI-04: alegação sem prova apontável). Corrigido apontando para o artefato REAL e datado que mede isso — `_evolucao-skills/rodadas/onda-descriptions-2026-07-18-notas.md` (2026-07-18): eval de disparo por description com 2 roteadores cegos contra as 159 frases do GUIA; as 3 frases de `java-service-usecase` não estão entre os 2 vermelhos daquela onda e o estado final é "159/159 verde" — `acionou = S` por eliminação/estado final documentado, não por linha explícita para esta skill (declarado assim, sem inflar a certeza). Nota também deixa explícito que os casos 1–3 do placar tiveram o pós-skill carregado à força (não disparo orgânico), então a coluna "Acionou" reflete o eval de disparo separado, não estas sessões. `description` intocada.
- **2026-07-19 — Rodada 2 (destravamento C1, decisões D-A/D-B do Jeremias):** Guardrails reescritos — os 3 bullets repetiam os invariantes em negativo (duplicação, PADRAO §12.5); agora cada um agrega nuance nova: RO-01 com artefatos específicos (exceção/injeção/lib que a casa não usa), consistência interna de sinalização de falha dentro do MESMO serviço (não coberta antes), e a fronteira dupla-transação × RO-10 (JDBC seguro, conexão única não thread-safe). Bullets "Como a falha é sinalizada" e "Forma de valor composto" deixaram de citar o SIGO verbatim (mini-cópia de `referencia-exemplos-reais-sigo.md`) e viraram remissão às convenções 5/6 da referência (fonte única). `evals/evals.json`: todos os 3 casos ganharam `origem: real|sintetico` + `data`/`sessao` (RO-01 — nunca "real" sem fonte apontável); caso 2 ("outra casa") deixou de ser hipótese solta e passou a ancorar em código REAL do Gradup (`CatalogService` — injeção por construtor + record de leitura, verbatim em `springboot-repository-service/referencia-exemplos-reais-gradup.md`; exceção de negócio via `orElseThrow(IllegalArgumentException)`, convenção real citada em `springboot-repository-service/SKILL.md`), com a divergência entre a description "Java desktop" desta skill e o contexto Java web do caso declarada explicitamente no próprio caso (RI-04), sem tocar a description. `description` intocada. RO-10 citado no texto vigente pós-decisão D-C (JDBC seguro — resiliência/retry na forma que o projeto pratica, sem impor `RetryDB`).
- **2026-07-19 — Correção pós-Painel 1 (receita espelhar-não-impor):** parágrafo "Gabarito SIGO" reduzido a remissão à referência (parava de reexpor em prosa as 9 convenções já listadas em `referencia-exemplos-reais-sigo.md` e já citadas nos bullets de "O que VARIA por projeto" — duplicação corpo↔corpo e corpo↔referência apontada por 3 juízes); corrigida a afirmação de que a referência traz `AutenticacaoService`/`AlertasService` verbatim — só o `AutenticacaoService` é verbatim, o `AlertasService` é prosa (apontado por 2 juízes); removida a duplicata verbatim de "não duplicar validação que já está na entidade" em "Regras de implementação" (já coberta em "Os invariantes inegociáveis"). Efeito líquido: corpo mais curto, sem perda de regra.
- **2026-07-19 — Evolução ao 9,5 (receita espelhar-não-impor, núcleo SIGO):** corpo reestruturado em **invariantes inegociáveis** (validação de negócio, sem SQL/UI no serviço, atomicidade só multi-passo, RO-01) × **o que varia por projeto** (pacote/idioma, forma de resolução de dependência, sinalização de falha, onde vive o log, forma de valor composto, se há teste de serviço). Resolvidas as fricções que a própria baseline (2026-07-18) já diagnosticara e o corpo ainda não corrigira: pacote genérico "service"/"métodos em inglês" × pacote real PT-BR do SIGO; "dependências recebidas por construtor" × campos diretos sem injeção do SIGO; "propagar exceção de negócio coerente" × falha esperada = `null` no SIGO; "RO-08 Log4j2 no serviço" (regra fixa) × `throws SQLException` propagado sem logger no SIGO. Verificação de fechamento (RI-04) tornada condicional: teste com dublês só se o projeto pratica, senão SKIP declarado — a referência SIGO não traz exemplo de teste de serviço. Eval em `evals/evals.json` (3 casos: SIGO → serviço espelha campo direto/retorno null/sem log; projeto genérico com injeção+NegocioException+logger no serviço → espelha ESSE projeto; simetria — projeto que testa serviço com dublês reais → segue o teste, não SKIP).
- **2026-07-18 — Few-shot de código real (escala do piloto, onda SIGO):** criada `referencia-exemplos-reais-sigo.md` (fonte: `AutenticacaoService.java` + `AlertasService.java`). Baseline §11: geração sem gabarito divergiu em pacote, injeção por construtor, NegocioException inventada, logger no serviço e validate() na entidade. Degrau §6.10: 2 (referência nova). Notas em `rodadas/onda-fewshots-2026-07-18-notas.md`.
