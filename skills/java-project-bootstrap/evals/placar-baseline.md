# PLACAR-BASELINE — java-project-bootstrap

**Data:** 2026-07-19
**Rodada:** C1 Onda B

> **acionou:** N/D — esta rodada NÃO tem a invocação visível no transcript da sessão de eval que o PADRAO-DE-AUTORIA §11.5 exige como evidência de disparo espontâneo. A skill foi carregada manualmente (carga forçada) para gerar o pós-skill; o "S" que aparecia antes nesta nota era **inferido** do estado 159/159 verde da onda de descriptions de 2026-07-18 (`_evolucao-skills/rodadas/onda-descriptions-2026-07-18-notas.md`), um lote não relacionado a este eval — não cumpre o critério de §11.5 e não deve ser lido como acionamento comprovado. Recaptura com prompt-gatilho real (sem nomear a skill) e transcript de disparo fica pendente.

| caso | origem | baseline | pos-skill | acionou | aderiu | observação |
|---|---|---|---|---|---|---|
| 1 | real (SIGO/sistema-cot) | 🔴 vermelho | 🟢 verde | N/D (carga forçada; ver nota acima) | sim | Baseline usa JavaFX 21.0.2 e Java 17 source/target (não 21.0.4/`release=21` do gabarito real), aponta o mainClass do plugin JavaFX direto para `App` sem qualquer menção a `Launcher`/`maven-shade-plugin`/`ServicesResourceTransformer`/exclusão de `module-info.class` e assinaturas, e não fala em `.gitignore` para `config.properties` nem em CSS placeholder sem hex — falha as 5 assertions checáveis. Pos-skill espelha o pom real do sistema-cot (Java 21, JavaFX 21.0.4, UCanAccess 5.0.1, Log4j2 2.23.1), aponta o shade para `Launcher` neutro (evita "JavaFX runtime components are missing"), cita o `maven-shade-plugin` como obrigatório, versiona `config.properties.example` com `.gitignore` cobrindo o real, e entrega CSS placeholder zero hex com comentário apontando para `javafx-theme-tokens`. Único ponto não detalhado explicitamente no texto do pos-skill: `ServicesResourceTransformer` e a exclusão fina de `*.SF/*.DSA/*.RSA` não são citados por nome (o shade é mencionado como presente, não com esse nível de detalhe) — não derruba o veredito verde, mas fica como lacuna de evidência textual. Caso claramente discriminante. |
| 2 | sintético (outra casa, Postgres) | 🔴 vermelho | 🟢 verde | N/D (carga forçada; ver nota acima) | sim | Baseline acerta o que a skill deveria proteger por padrão neste caso: driver PostgreSQL (não UCanAccess por reflexo do SIGO), nenhum `jbcrypt`/`atlantafx-base`/`openhtmltopdf-pdfbox`, pacote `com.vistoriapro.app` correto, e não cria `Database`/`Backup`/`seedAdminInicial`/`login.fxml`. Mas erra o único invariante universal do gabarito testado aqui: o `javafx-maven-plugin` aponta o mainClass direto para `MainApp` (que estende `Application`), sem `Launcher` neutro nem `maven-shade-plugin` — geraria "JavaFX runtime components are missing" ao rodar `java -jar`. Pos-skill corrige exatamente esse ponto (`App` + `Launcher` neutro apontado no shade) mantendo driver/pacote/ausência de extras corretos herdados do baseline. Caso parcialmente discriminante — só no invariante Launcher, mas é o que a skill existe para garantir. |
| 3 | sintético (greenfield, sem projeto-irmão) | 🔴 vermelho | 🟢 verde (com ressalva) | N/D (carga forçada; ver nota acima) | sim | Baseline acerta o checklist negativo (sem `jbcrypt`/`atlantafx`/`openhtmltopdf`, não assume Access/UCanAccess sem confirmação, usa `br.com.acme.desk`/"Acme Desk" corretos, não cria `Database`/`Backup`/`seedAdminInicial`/`login.fxml`), mas configura o `javafx-maven-plugin` com mainClass direto para `App` — sem `maven-shade-plugin` nem `Launcher` neutro — e declara "Suposições assumidas" em prosa solta, sem o rótulo `SUPOSIÇÃO:` nem rodar build de fechamento. Pos-skill aplica o checklist negativo com rigor explícito (nomeia item a item o que do gabarito SIGO NÃO está copiando: jbcrypt, atlantafx, openhtmltopdf, banco padrão, Database/Backup/seed/login, mainClass direto em App), planeja `Launcher` neutro no shade e pergunta as versões (Java 21/JavaFX 21.0.4) antes de gerar os arquivos. Ressalva: a captura desta execução termina no pedido de confirmação — não mostra o `mvn -q -DskipTests package` verde que a assertion 6 exige como evidência de fechamento da SUPOSIÇÃO. A intenção e o plano estão corretos e aderentes à skill; a evidência de build executado fica pendente nesta rodada específica. |

## Leitura honesta

Os três baselines convergem no mesmo ponto cego: nenhum usa `maven-shade-plugin` com `Launcher` neutro como mainClass, apontando o plugin JavaFX direto para a classe `Application` — um defeito que, empacotado como fat-jar, produz o erro clássico "JavaFX runtime components are missing". No caso 1 (real, SIGO) o baseline erra quase tudo (versões, shade, gitignore, CSS), tornando o caso fortemente discriminante. No caso 2 o baseline já acerta a maior parte das decisões de "outra casa" (driver, pacote, ausência de extras não pedidos) e só erra o invariante Launcher — discriminação mais fina, mas real. No caso 3 o baseline segue bem o checklist negativo de greenfield (não importa Access/jbcrypt/atlantafx por reflexo), mas repete o mesmo erro de Launcher e não formaliza a SUPOSIÇÃO com o rótulo nem com build de fechamento. Os três pos-skill corrigem o padrão Launcher e mantêm as decisões corretas de cada casa (espelhar sem impor, RO-01), com uma ressalva legítima no caso 3: a execução capturada parou no pedido de confirmação ao usuário antes de rodar o `mvn package`, então a evidência de "build verde confirmando a SUPOSIÇÃO" (assertion 6) não está no texto desta rodada — vale recapturar uma execução completa até o fechamento antes de fechar 100% este caso como verde pleno. Além disso, os três casos ficam com `acionou: N/D` — este placar comprova baseline×pós-skill (aderência), mas não comprova acionamento espontâneo no sentido do PADRAO §11.5 (invocação visível no transcript sem nomear a skill); essa evidência específica ainda está pendente de captura.
