# Histórico — java-project-bootstrap

Registro de rodadas de evolução da skill (movido do corpo do SKILL.md para progressive disclosure; conteúdo verbatim).

- **2026-07-19 — Correção pós-painel (defeitos maiores da rodada C1/Onda B):** quatro ajustes. (1) Duplicação real entre o checklist negativo (itens 1 e 5) e o Guardrails (bullet 3, quase palavra por palavra) — Guardrails podado para remissão ("ver checklist negativo acima"), fechando o modo de falha 5 (PADRAO §12.5) que a poda anterior tinha deixado passar nesse ponto. (2) "identificadores em inglês" removido do bullet de invariantes: não tinha lastro em REGRAS-DE-OURO.md e contradizia a própria `referencia-exemplos-reais-sigo.md` (item 6, "Comentários e identificadores em PT-BR", tirado do código real do SIGO) — trocado por texto que não inventa regra de idioma e remete à referência real (RO-01). (3) `evals/placar-baseline.md`: a coluna `acionou` trazia "S (inferido...)" nos 3 casos; corrigida para "N/D (carga forçada; ver nota acima)" porque a inferência vinha de um lote não relacionado (onda de descriptions 2026-07-18) e não é a "invocação visível no transcript da sessão de eval" que o PADRAO §11.5 exige — ressalva agora também em cada célula da tabela, não só na nota de rodapé, e reforçada na "Leitura honesta"; recaptura com transcript real fica pendente. (4) Entrada de Histórico de 2026-07-19 (abaixo) corrigida: dizia que o placar-baseline "não foi rodado nesta execução", mas `evals/placar-baseline.md` já existia completo (3 casos, vermelho→verde) na mesma rodada — texto desatualizado frente ao próprio artefato; reescrito para refletir o que de fato existe (placar rodado, com a ressalva do item 3 acima).
- **2026-07-19 — C1/Onda B, receita espelhar-não-impor:** corpo reestruturado em **invariantes inegociáveis** (estrutura, pom mínimo, padrão `Launcher` neutro no shade — promovido de "few-shot enterrado" a invariante técnico universal de JavaFX+shade, segredos fora do git, build verde) × **o que varia por projeto** (driver de banco, dependências extras do domínio como jbcrypt/atlantafx/openhtmltopdf, plugins auxiliares, ordem de boot de feature, nome do pacote/app). Resolvida a fricção corpo↔referência (alavanca 1): o padrão `Launcher`/shade, antes só na `referencia-exemplos-reais-sigo.md`, agora está no corpo genérico como invariante com o porquê técnico ("JavaFX runtime components are missing"), então não há mais divergência entre o que o corpo genérico ensina e o que o gabarito real usa. Adicionado **checklist negativo** explícito (lição dos casos vermelhos de greenfield do `java-db-foundation`/`javafx-dashboard`, `rodadas/R-lote2-ondaA-2026-07-19.md`): o que NÃO importar do SIGO por reflexo em projeto sem fonte real (jbcrypt/atlantafx/openhtmltopdf, banco Access, `Database`/`Backup`/`seedAdminInicial`/`login.fxml`, pacote `br.com.cot`, os três plugins). "Leituras obrigatórias" ganhou critério concreto de confirmação (antes "confirmável" sem critério — fraqueza nº 1 da auditoria 2026-07-13): declarar `SUPOSIÇÃO:` e confirmar pelo build verde. Placeholder FXML/CSS ganhou definição de "mínimo" (antes indefinido): zero hex fixo, comentário apontando para `javafx-theme-tokens`. Nova seção **Verificação de fechamento (RI-04)** com SKIP declarado honesto quando não há Maven/JDK na sessão. Guardrails podados para só nuance nova (anti-duplicação, PADRAO §12.5) — removidas as 2 linhas que só repetiam segredos-fora-do-git e "não afirmar versão de cor", já cobertas em Leituras obrigatórias/invariantes. Citação `desvio se declara (RI-01)` corrigida para `(RI-04)` — RI-01 é co-responsabilidade/ADR, RI-04 é rastreabilidade/evidência (mesma correção sistêmica já aplicada em `java-db-foundation`/`java-jdbc-dao`; ver `_evolucao-skills/rodadas/R-lote2-ondaA-2026-07-19.md`, achado 1), corrigida também em `referencia-exemplos-reais-sigo.md`. `evals/evals.json` criado (3 casos: SIGO real com fonte, "outra casa" sintética sem UCanAccess/extras do SIGO, greenfield com mandato endurecido via checklist negativo) — todos com `origem`/`fonte`/`data`/`sessao`. `description` mantida byte-idêntica (congelada). `evals/placar-baseline.md` rodado nesta execução (3 casos, vermelho→verde) com uma ressalva honesta: a coluna `acionou` não tem a invocação visível no transcript da sessão de eval que o PADRAO §11.5 exige como evidência — é inferida de outra onda (descriptions, 2026-07-18), sob carga forçada; declarado como tal na própria tabela, não fabricado (RI-04). Recaptura com transcript real de disparo espontâneo fica como pendência.
- **2026-07-18 — Few-shot de código real (2ª leva Java, código do SIGO):** criada `referencia-exemplos-reais-sigo.md` (fonte: código vivo do SIGO/sistema-cot). Baseline §11: sem o gabarito o gerador diverge nos pontos-chave da casa (ver referência). Degrau §6.10: 2 (referência nova). Notas em `rodadas/onda-fewshots-2026-07-18-notas.md`.
