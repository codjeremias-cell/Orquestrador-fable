# Histórico de autoria — spec-javafx-new-system

Registro das rodadas de evolução desta skill. Movido para cá (fora do SKILL.md) para manter o corpo do orquestrador enxuto; consulte quando precisar auditar como a skill chegou ao estado atual.

- **2026-07-19 — Receita espelhar-não-impor (C1/Onda B):** endereçada a fraqueza nº1 da auditoria de 2026-07-13 ("gates frouxos nas etapas 4-5; fronteira unilateral com spec-desktop-app"): etapas 4 (Logging) e 5 (Tema) ganharam critério de conclusão checável (log gravado de verdade, app abre nos dois modos sem hex hardcoded); Rede ganhou `spec-desktop-app` em "Não confundir com" (ele cita esta skill e fazia handoff pra cá, mas esta não citava de volta). Adicionada seção própria `## Verificação de fechamento (RI-04)` com SKIP declarado explícito (empacotamento sem jpackage/Windows, banco de teste inacessível). "Regras de coerência" ganhou a fronteira invariante (ordem 1→8 e gates) × o-que-varia (decisão técnica de cada etapa é da skill filha) — sem duplicar o corpo delas. `evals/evals.json` reescrito para os 3 casos da receita (SIGO real com fonte · outra-casa sintética declarada · greenfield com **mandato endurecido**: checklist NEGATIVO explícito do que não importar do SIGO por reflexo — lição direta do achado 3 e do caso 3 vermelho de `java-db-foundation` registrados em `rodadas/R-lote2-ondaA-2026-07-19.md`); os 3 casos antigos (não-aciona-em-projeto-existente, para-em-entrada-ambígua) saem do arquivo — cobertura substituída pela composição pedida nesta rodada, não avaliada de novo aqui. `evals/placar-baseline.md` (medição de 2026-07-09) passa a valer só para o eval set anterior; **correção (mesmo dia, pós-consolidação):** a pendência de reexecução foi fechada dentro do próprio arquivo pela seção `## Consolidação C1 Onda B — 2026-07-19` — leitura direta das 6 saídas reais contra as `assertions` de `evals.json` (rater único, sem par cego, diferente do método pareado de 2026-07-09). Ressalva que permanece aberta: a coluna `Acionou` das 3 linhas usa evidência indireta herdada da onda de descriptions de 2026-07-18 (inferência declarada), não "a invocação visível no transcript da sessão de eval" exigida pelo §11.5 do `PADRAO-DE-AUTORIA.md` — gap sinalizado no próprio placar (marcado com † na tabela), não escondido, e não fechável aqui sem reexecutar os 3 casos com captura de transcript (fora do escopo desta edição de arquivo). Citações de regra conferidas contra o texto vigente de `REGRAS-DE-OURO.md` (RO-10 reescrita 2026-07-19 já refletida no passo 3 desde a Onda A; nenhuma citação `RI-01` indevida encontrada nesta skill). `description` intocada (nenhuma contradição com o corpo novo).
- **2026-07-18 — Exemplo montado real (orquestrador):** criada `referencia-exemplo-montado-sigo.md` mapeando a feature/sistema real (SIGO/Gradup) através da sequência que este orquestrador encadeia — capstone que amarra os few-shots das skills do track. Degrau §6.10: 2 (referência nova). Notas em `rodadas/onda-fewshots-2026-07-18-notas.md`.
