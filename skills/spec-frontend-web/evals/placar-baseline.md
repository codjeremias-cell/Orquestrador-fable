# Placar baseline × com-skill — spec-frontend-web

> Medição do §11 do PADRAO-DE-AUTORIA - **2026-07-09**. Executor: **Sonnet** (baseline sem acesso ao catálogo vs. com a SKILL.md carregada). **Juiz cego** por eval (ordem A/B alternada), pontuando 1 / 0,5 / 0 por expectation; score = fração das expectations atendidas. Workflow `placar-baseline-orquestradores` (run `wf_8db22f26-e56`, 84 agentes, 28 evals, 0 erros).

| Eval | Baseline | Com skill |
|---|---|---|
| frontend-completo-sobre-backend-pronto | 0,25 | 1,00 |
| nao-aciona-para-etapa-isolada | 0,17 | 1,00 |
| sistema-inteiro-prefere-universal | 0,00 | 1,00 |
| **Média** | **0,14** | **1,00** |

## Observações do juiz (evidência RI-04)

- **frontend-completo-sobre-backend-pronto:** Resposta A é um plano de execução competente e genérico (padrão dev-senior/arquiteto), com boas práticas reais (contrato via Swagger, Zod para forms, testes E2E, checklist 'Para testar'), mas NÃO segue a sequência de gates da skill spec-frontend-web: não há mockups aprovados antes do código, não há frontend-stack-decisor/ADR formal, não há geração de tokens DTCG antes dos componentes, não há gate de a11y (axe), não há testador-real com axe/relatório datado, e não há iteração via Comitê/veredito do auditor. Resposta B segue rigorosamente a skill, cita as etapas e gates nominalmente (RO-06, mockups, ADR via frontend-stack-decisor, tokens DTCG sem hex solto, a11y axe+teclado, Zod contra DTO real, testador-real Playwright+axe com relatório datado, Comitê ≥9, veredito do auditor) e corretamente trava a execução por falta do contrato real do backend, pedindo os dados necessários — coerente com a condição de parada obrigatória da própria skill.
- **nao-aciona-para-etapa-isolada:** Resposta A cumpre exatamente o esperado: identifica o pedido como etapa isolada citando a cláusula de exclusão da própria skill, recusa rodar o orquestrador completo e redireciona explicitamente para web-component, sem impor descoberta/stack/tokens (assume que os tokens do design system já existem). Resposta B nunca menciona ou aciona a skill web-component — em vez disso conduz ela mesma um processo extenso de perguntas de esclarecimento (stack web vs JavaFX, localização dos tokens, variantes, padrão de a11y) antes de "escrever qualquer código", o que é precisamente o tipo de imposição de descoberta que a expectation 3 pede para NÃO fazer em um pedido pontual. Ela reconhece parcialmente o escopo isolado (recusa expandir para integrar em telas), mas não traduz isso em roteamento correto para a skill filha, então falha nas expectations 2 e 3 e atende só parcialmente a 1.
- **sistema-inteiro-prefere-universal:** Resposta A nunca menciona a skill spec-frontend-web nem spec-projeto-completo; trata o pedido como uma consultoria genérica de condução de projeto e efetivamente monta um plano de execução cobrindo backend (Onda 1), frontend (Onda 2) e deploy (Onda 3) com decisões de arquitetura para todos eles — ou seja, conduz backend e deploy em vez de reconhecer a fronteira da skill e redirecionar. Não atende nenhuma das três expectations (que são especificamente sobre reconhecer o limite do orquestrador de frontend e apontar para spec-projeto-completo). Resposta B cita literalmente as regras da skill (cabeçalho, seção de escopo, 'não confundir com'), identifica que o pedido nomeia backend+frontend+deploy 'do zero' como o caso de exclusão, recusa executar qualquer etapa da sequência e direciona explicitamente para spec-projeto-completo, sem tentar conduzir backend/deploy. Atende plenamente as três expectations.
