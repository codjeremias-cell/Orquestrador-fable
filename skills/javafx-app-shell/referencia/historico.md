# Histórico de evolução — javafx-app-shell

Changelog detalhado da skill, movido para fora do `SKILL.md` (progressive disclosure). Conteúdo preservado verbatim.

- **2026-08-11 — Fronteira fora da zona de corte (proveniência: `_auditoria/zelador-inventario-2026-08-10.md`, item 28 e §MELHORAR):** o discriminador contra as três irmãs morava nos últimos 254 dos 644 caracteres da description — exatamente a cauda que o teto de listagem corta primeiro. O bloco "Fronteira entre as skills-irmãs" foi colapsado numa oração curta logo após a primeira frase ("Aqui é a CASCA do app: tela de feature é javafx-screen-fxml, painel de KPIs é javafx-dashboard, tema por tokens é javafx-theme-tokens"), passando o discriminador do offset 390 para o 170 — dentro dos primeiros ~250 chars. Os seis gatilhos entre aspas ("AlertaUtil", "cria a janela principal do app", "monta a navegação entre telas", "preciso do menu e da estrutura base da interface", "cria o AlertaUtil e o controller base", "monta o esqueleto/a casca da UI") foram preservados byte a byte; total 644 → ~512. Corpo intocado — a §"Fronteira com as skills-irmãs" segue com o detalhe completo.
- **2026-07-19 — Correção pós-painel (defeitos maiores da rodada C1/Lote 2):** três ajustes de corpo, todos apontados pelo próprio `evals/placar-baseline.md` desta rodada. (1) RO-12 ganhou o mesmo destaque estrutural de RO-08/RO-09/RO-J1 no bullet de invariantes — agora nomeia o efeito de falha (variável não declarada em `.root` é ignorada em silêncio) e exige que a saída mencione explicitamente quais tokens o shell consome, fechando o gap do caso 2 (pós-skill não mencionava tokens de tema). (2) "Regra de partida", "Entradas obrigatórias", "Trava obrigatória" e o passo 3 do Fluxo agora dizem explicitamente que dado faltante (nome do app, itens de navegação) declara **SUPOSIÇÃO:** e segue para o mockup provisório, em vez de bloquear a entrega — fecha a pendência de calibração nomeada pelo próprio placar (caso 3, "Leitura honesta": "declare e proponha" deveria vencer "bloqueie até ter todos os dados"). (3) `evals/placar-baseline.md` teve a coluna `acionou` corrigida: o valor anterior ("S", com inferência de outro lote) foi trocado por "indeterminado" com a lacuna declarada — o §11.5 do `PADRAO-DE-AUTORIA.md` exige "invocação visível no transcript da sessão de eval" como evidência de acionamento, e a sessão desta rodada não capturou esse transcript; a inferência a partir do estado final 159/159 verde da onda de descriptions de 2026-07-18 (lote não relacionado) não substitui essa evidência. Esse item permanece como pendência real — só se fecha re-rodando os evals com transcript capturado, não com correção de texto.
- **2026-07-19 — Evolução ao 9,5 (receita espelhar-não-impor, rodada C1/Lote 2):** corpo reestruturado em **invariantes inegociáveis** (alerta centralizado, sem regra de negócio no shell, RO-09/RO-08/RO-J1, estados cobertos, tokens de tema, identidade) × **o que varia por projeto** (nome e forma da classe de alerta, existência do controller-base, onde vive a aplicação do tema, mecanismo de navegação, ordem de boot). Atacada a fraqueza nomeada na auditoria (2026-07-13, "borda de tema inexistente"): o corpo agora separa explicitamente o que o shell CONSOME de `javafx-theme-tokens` do que ele próprio decide (botão de troca vs. lógica de troca). Essa correção resolve a fronteira estrutural (o que é escopo de cada skill) — **não** implica que RO-12 já apareça garantido em todo output pós-skill: o `evals/placar-baseline.md` (caso 2, mesma rodada) registrou que o pós-skill daquela sessão não mencionou tokens de tema em nenhum ponto, gap real que ficou pendente até a correção pós-painel acima. Removida a fricção corpo↔referência: o corpo mandava genericamente "AlertaUtil" enquanto o gabarito SIGO real chama a classe de `Dialogos` — o corpo agora nomeia "AlertaUtil" como rótulo genérico e manda espelhar o nome real. Duas citações de RO conferidas contra o texto vigente de REGRAS-DE-OURO.md e corrigidas por excesso de atribuição: RO-12 (só rege sintaxe de CSS/tokens, não "troca claro/escuro" — a alternância virou "o que varia") e RO-J1 (só cobre "não congelar" com `Task`/placeholder "Carregando…", não os estados vazio/erro — que vêm do Padrão universal "Estados sempre cobertos", citado à parte). Verificação de fechamento (RI-04) nova, específica: evidência de abertura real do app (print/log) com **SKIP declarado** quando não há ambiente gráfico na sessão — ataca a fraqueza "evidência 'abrir o app' não auditável". `evals/evals.json` criado (3 casos, `origem` em todos): SIGO → `Dialogos`/`Carregar`/ordem de boot real; outra casa (sintético, sem fonte real de segunda casa JavaFX no cofre) → espelha `AlertaUtil`/`BaseController` reais dessa casa; greenfield (sintético) → `SUPOSIÇÃO:` declarada para nome de classe e controller-base, com mandato explícito para NÃO importar `Dialogos`/`Carregar`/a ordem de boot do SIGO por reflexo (lição do caso 3 vermelho de `java-db-foundation`).
- **2026-07-18 — Few-shot de código real (2ª leva Java, código do SIGO):** criada `referencia-exemplos-reais-sigo.md` (fonte: código vivo do SIGO/sistema-cot). Baseline §11: sem o gabarito o gerador diverge nos pontos-chave da casa (ver referência). Degrau §6.10: 2 (referência nova). Notas em `rodadas/onda-fewshots-2026-07-18-notas.md`.
