# Histórico de evolução — javafx-screen-fxml

Changelog detalhado da skill, movido para fora do `SKILL.md` (progressive disclosure). Conteúdo preservado verbatim.

- **2026-07-19 — Correção pós-painel (prova de acionamento sem fonte rastreável):** `evals/placar-baseline.md` citava, nas 3 células da coluna "acionou", e a entrada de Histórico abaixo citava, um rastreio `_evolucao-skills/rodadas/` sessão `C1-lote2-ondaA` que não existe como arquivo no cofre (conferido por listagem do diretório e busca textual — "C1-lote2-ondaA" só aparece dentro dos próprios artefatos desta skill, nunca num arquivo de sessão real). A evidência era, na prática, inferida do estado final de uma onda de eval diferente (`onda-descriptions-2026-07-18-notas.md`, disparo por description contra as 159 frases do `GUIA-DE-CHAMADAS-SKILLS.md`, não a execução destes 3 prompts específicos). Corrigido para citar esse artefato real diretamente, com a cadeia de inferência declarada por extenso (as 3 frases de `javafx-screen-fxml` no guia não estão entre os 2 vermelhos daquela onda; estado final "159/159 verde"), deixando claro que é medição de disparo por description (roteador cego, top-1), não invocação observada em transcript destes 3 casos — que foram carregados à força, não disparados organicamente. `description` intocada.
- **2026-07-19 — Evolução ao 9,5 (receita espelhar-não-impor, C1/Lote 2):** corpo reestruturado em **invariantes inegociáveis** (UI não pode travar perceptivelmente, RO-09, estados sempre cobertos, RO-12, RO-08, controller fino, RO-05/PT-BR) × **o que varia por projeto** (o MECANISMO anti-travamento). Removida a fricção "corpo mandava sempre `Task` (RO-J1) × o gabarito real do SIGO (`ComunicadoFormController.aoSalvar`) chama o DAO de forma síncrona para escrita simples de formulário" — o corpo genérico contradizia a própria referência que cita como fonte; agora a skill lê como o projeto trata a escrita de form e espelha (Task só é o padrão seguro por falta de exemplo, ou em cargas mais pesadas). Guardrails podados de 5 para 2 (removidas 3 repetições em negativo de invariantes já ditos; mantido só o que agrega nuance nova). Adicionada **Verificação de fechamento (RI-04)** — a skill entregava tela sem nenhuma evidência de fechamento (fraqueza nomeada na auditoria de 2026-07-13, nota 7,9: "EVIDÊNCIA AUSENTE: entrega tela sem compilar/abrir nada"); a seção nova espelha o que a `javafx-dashboard` do mesmo track já pratica (compilar + conferir binding `fx:id`↔`@FXML`), com SKIP declarado quando não há projeto rodável na sessão. Corrigido "Mestre" → "Jeremias" na Regra de partida (RO-06), alinhado à convenção do cofre (CLAUDE.md). `evals/evals.json` criado (3 casos: SIGO → forma síncrona real do form; outra casa sintética com Task mesmo em forms simples → espelha essa casa mantendo os invariantes; greenfield → SUPOSIÇÃO declarada, mandato explícito para não importar o padrão SIGO por reflexo — lição do caso 3 vermelho da `java-db-foundation`). Description mantida byte-idêntica (congelada). Rastreio desta rodada de evolução: sessão `C1-lote2-ondaA` (rótulo interno da rodada — sem arquivo de sessão próprio no cofre); evidência de acionamento das 3 skills do eval é o artefato separado `_evolucao-skills/rodadas/onda-descriptions-2026-07-18-notas.md` (ver nota em `evals/placar-baseline.md`).
- **2026-07-18 — Few-shot de código real (escala do piloto, onda SIGO):** criada `referencia-exemplos-reais-sigo.md` (fonte: `comunicado_form.fxml` + `ComunicadoFormController.java`). Baseline §11: geração sem gabarito divergiu em path/naming do FXML, pacote do controller, handlers on→ao, notação húngara, AlertaUtil×labelErro e service×DAO direto em form simples. Degrau §6.10: 2 (referência nova). Notas em `rodadas/onda-fewshots-2026-07-18-notas.md`.
