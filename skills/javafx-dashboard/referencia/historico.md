# Histórico de evolução — javafx-dashboard

Changelog detalhado da skill, movido para fora do `SKILL.md` (progressive disclosure). Conteúdo preservado verbatim.

- **2026-07-19 — Mini-fechamento: endurecimento do mandato greenfield (checklist negativo):** por orientação do Jeremias, endurecido o mandato greenfield desta skill (mesma família do caso 3 vermelho de `java-db-foundation`, onde greenfield importou o gabarito SIGO por reflexo citando RO-01/RI-01 como se fosse conformidade). **Nota de verificação:** ao ler `evals/placar-baseline.md` desta skill para localizar o caso apontado, o caso greenfield real é o **id 3** (`greenfield-sem-projeto-irmao`) — já registrado **verde/aderiu=sim** no placar de 2026-07-19 (cumpriu o mandato SUPOSIÇÃO: corretamente); o **id 1** (`sigo-sigcot-executivo-saude-operacao`) é o caso real do SIGO, vermelho por um motivo diferente (bloqueio indevido da construção, tratado na entrada de Histórico anterior) — não por importação do SIGO por reflexo. O pedido desta rodada citava "caso 1 (greenfield)", o que não bate com a numeração atual do `evals.json`; endurecimento aplicado ao caso que de fato é greenfield (**id 3**) como reforço preventivo da mesma família de risco, não como correção de um vermelho existente neste arquivo. Ações: (1) `SKILL.md` ganhou a seção **"Greenfield sem projeto-irmão — checklist negativo"** logo após "O que varia por projeto — espelhe, não prescreva" (ponto onde o executor decide token/helper/refresh/temas) — lista explícita do que NÃO importar do SIGO por reflexo (`-sigo-*`, `AlertHelper`, `HoverDetalhe`, refresh 30 min, 4 temas, `SvgIcons`/`EstadoTabela`/`ScalingRoot`/`com.sigo.*`, ordem de boot/estrutura de pastas do SIGO). (2) `evals/evals.json` caso id 3: prompt tornado inequívoco (projeto nomeado "TarefasFlow", frase explícita "não herda nada do SIGO/SIGCOT") e adicionada uma assertion negativa explícita (ausência literal dos nomes do SIGCOT na resposta, salvo para dizer que não serão usados). **Description intocada.**
- **2026-07-19 — Correção pós-painel (3 achados: 2 maiores + 1 bloqueador):** **(bloqueador)** `evals/placar-baseline.md` existia no disco com data/sessão idênticas (`2026-07-19`, `C1-lote2-ondaA`) e tabela completa dos 3 casos, contradizendo a frase "placar-baseline.md não gerado nesta rodada — SKIP declarado" na entrada de Histórico abaixo; corrigida essa frase para bater com o artefato real (ver nota inline abaixo). Ressalva mantida sem correção (não corrigível sem fabricar evidência — RI-04/RO-01): a coluna "acionou" do placar continua sendo uma inferência declarada de outra rodada (afinação de descriptions de 2026-07-18), não uma medição direta destes 3 prompts; isso já estava honestamente sinalizado em "Leitura honesta" do próprio placar e segue assim. **(maior ×2)** O caso 1 do placar (único `origem: real`, `sigo-sigcot-executivo-saude-operacao`) segue vermelho pós-skill: a execução bloqueou a construção alegando "não localizei o projeto... não há DAO real acessível" apesar do prompt já dizer "considere o mockup pré-aprovado" (RO-06 satisfeita) e da skill empacotar exatamente `referencia/sigcot-stack.md` + `referencia-exemplos-reais-sigo.md` para este cenário — nenhum dos nomes da referência (tokens `-sigo-*`, `AlertHelper`, 4 temas, `HoverDetalhe`, `Task`+record `Dados`) apareceu na resposta. Corrigido em "Entradas obrigatórias" #3 e "Etapa 4 — Construir": agora ambos deixam explícito que, com mockup aceito e a referência real empacotada disponível, ela já é fonte suficiente para construir no padrão SIGCOT sem repositório vivo aberto na sessão — só a assinatura exata de um DAO/query específico e novo, fora do que a referência cobre, exige `SUPOSIÇÃO:` pontual (RO-01), sem justificar recusar a construção inteira. **Description intocada**, conforme instrução desta rodada. `referencia/*.md` e `evals/*` não alterados nesta correção (achados eram todos do corpo do `SKILL.md`).
- **2026-07-19 — C1/Lote 2 (receita espelhar-não-impor, desSIGOtização — decisão Jeremias):** corpo reestruturado em **invariantes inegociáveis** (RO-J1, RO-12 cor-por-token, RO-09, RO-08, RO-05, estados, densidade, controller fino) × **o que varia por projeto** (prefixo do token, contagem de temas, nome do helper de alerta, mecanismo de hover/detalhe, cadência de refresh, ícone, lib de gráfico) — ataca a fraqueza nº1 nomeada em `_auditoria/2026-07-13-notas-52-skills.md` (linha 62: "scope SIGCOT chumbado (§7)"). Citação de **RO-12** corrigida para não atribuir à regra algo que ela não diz (a obrigação de "virar em N temas" vira invariante próprio da skill, não extrapolação de RO-12 — defeito recorrente apontado no item 5 dos achados sistêmicos). **AlertaUtil × AlertHelper** (P5, ROADMAP): investigado — `AlertHelper` é o nome confirmado na própria referência real desta skill (`referencia/sigcot-stack.md`, `com.sigo.util`, extraído de código de produção); não há contradição interna. A divergência é **entre skills** (`javafx-app-shell` cita `AlertaUtil`/o real `Dialogos` de outro projeto/pacote) — fora do escopo desta rodada de skill única; segue rastreado em `ROADMAP.md` item P5 (3 skills). Corpo agora cita `AlertHelper` como gabarito espelhado do SIGCOT, não regra universal. **Fricção corpo↔referência (temas):** `referencia/sigcot-stack.md` e `referencia/blueprint-template.md` ainda diziam "6 temas" — desatualizado desde a correção do corpo para 4 (painel das 23, 2026-07-18); corrigido nos dois arquivos para bater com o corpo e com `javafx-theme-tokens`, com nota de cautela onde a lista de 6 arquivos CSS separados também pode estar obsoleta (não reescrita sem fonte real — RO-01; SKIP declarado). **"Mestre" → "Jeremias"** (3 ocorrências: `SKILL.md` ×2, `referencia/blueprint-template.md` ×1), alinhando à convenção do cofre (mesmo defeito nomeado em `spec-javafx-crud-feature`). **Duplicação eliminada** (§12.5): os blocos "Atualização padrão" e "HoverDetalhe" existiam **3 vezes** — verbatim no corpo (`Convenções de construção`) e em duas cópias idênticas (`referencia/widgets-extraido.md` e `referencia-widgets-extraido.md`, nenhuma referenciada de lugar nenhum — confirmado por busca no catálogo inteiro). Removida a cópia `referencia/widgets-extraido.md`; mantida `referencia-widgets-extraido.md` (padrão de nome raiz usado pelos few-shots do catálogo) e agora **linkada** no corpo (seção "O que varia por projeto"); o corpo parou de duplicar o texto integral, ficando com o resumo + ponteiro. Verificação de fechamento **nomeada explicitamente `## Verificação de fechamento (RI-04)`** com **SKIP declarado** honesto (sem projeto/app acessível = SKIP, nunca "compilou"/"virou nos temas" fingido) — segue o padrão de `java-jdbc-dao`/`java-db-foundation`. `evals/evals.json` reconstruído: 3 casos — SIGO/SIGCOT (`origem: real`), outra casa JavaFX sintética declarada (`origem: sintetico` — não existe segunda casa JavaFX com referência real no cofre, confirmado por busca), greenfield (`origem: sintetico`, mandato explícito para **não** importar `-sigo-*`/`AlertHelper`/`HoverDetalhe`/30 min por reflexo — lição do caso 3 vermelho de `java-db-foundation`) — todos com `fonte`, `data: 2026-07-19`, `sessao: C1-lote2-ondaA`. `placar-baseline.md` **gerado nesta rodada** (mesma sessão `C1-lote2-ondaA`), com os 3 casos de `evals.json` (baseline × pós-skill, citação comportamental verbatim de cada execução — não é SKIP). *Correção 2026-07-19 (painel pós-Lote 2): esta entrada do Histórico afirmava, por erro, "placar-baseline.md não gerado nesta rodada — SKIP declarado", contradizendo o próprio arquivo `evals/placar-baseline.md`, que já existia no disco com a mesma data/sessão e tabela completa. Corrigido aqui para bater com o artefato real; ver entrada de Histórico mais recente para o detalhe da correção e do achado que ela expôs (caso 1 vermelho pós-skill).* Rastreio: `_evolucao-skills/rodadas/` (painel a produzir na auditoria de fechamento).
- **2026-07-18 — Correção de consistência (painel das 23):** "6 temas" (estado antigo do SIGCOT) corrigido para **4 temas** (claro/escuro/cinza/grafite), batendo com o código real (`Tema.java`=4 Modo; `styles.css`=3 classes `.tema-*`+base) e com a `javafx-theme-tokens`. Defeito real apontado pelos 3 juízes.
- **2026-07-18 — Few-shot de código real (escala do piloto, onda SIGO):** criada `referencia-exemplos-reais-sigo.md` (fonte: `DashboardController.java`). Baseline §11: geração sem gabarito convergiu no Task+record (espírito já ensinado pelo corpo), mas divergiu em reuso do AlertasService, KPIs clicáveis/navegação injetada, naming húngaro e auto-refresh inexistente no real. Degrau §6.10: 2 (referência nova). Notas em `rodadas/onda-fewshots-2026-07-18-notas.md`.
