# PLACAR-BASELINE — java-package-desktop

**Data:** 2026-07-19
**Rodada:** C1 Onda B

> **Nota sobre "acionou".** S (inferido do estado 159/159 verde da onda de descriptions 2026-07-18, rastreio `_evolucao-skills/rodadas/onda-descriptions-2026-07-18-notas.md`; inferência declarada; execução com carga forçada). O artefato citado mediu disparo por DESCRIPTION (2 roteadores cegos ao gabarito escolhendo top-1 entre as 53 descriptions, contra as 159 frases-gatilho do `GUIA-DE-CHAMADAS-SKILLS.md`) e fechou com 159/159 verde após correção dos 2 únicos vermelhos daquela onda (`gradup-testador` e `java-project-bootstrap` — nenhum dos dois é `java-package-desktop`). Logo `acionou = S` por eliminação/estado final documentado no artefato, não por veredito individual linha-a-linha explícito desta skill. Os 3 casos abaixo tiveram o pós-skill executado por carga forçada (não disparo orgânico nesta sessão) — a coluna reflete o eval de disparo separado, não estas sessões.

| Caso | Origem | Baseline | Pós-skill | Acionou | Aderiu | Observação |
|---|---|---|---|---|---|---|
| 1 — SIGO/sistema-cot (fat-jar + jpackage + zip) | real | 🔴 vermelho | 🟢 verde | S | ✅ sim | Baseline inventa versão/config de plugin (`maven-shade-plugin 3.5.1`, filtros de exclusão) sem ler o `pom.xml`/`empacotar.ps1` reais, e falha 3 das 5 assertions: não cita `--add-modules java.se,jdk.unsupported`, não entrega `.zip` com retentativa (propõe apenas fat-jar e pergunta se deve seguir para jpackage), e não empacota `config.properties`/`docs/INSTALACAO.txt`. Pós-skill recusa inventar (o projeto não existe neste cofre), cita a fonte real (`referencia-exemplos-reais-sigo.md`) e reproduz os 4 passos corretos — `--main-class br.com.cot.Launcher`, `--add-modules java.se,jdk.unsupported`, fat-jar antes do jpackage, entregável final `.zip` com retentativa 5x/3s, `config.properties` + `docs/INSTALACAO.txt` — batendo as 5 assertions, e declara o bloqueio (falta o caminho real do repo) em vez de simular execução. Caso discriminante. |
| 2 — projeto irmão, só .msi via WiX | sintético | 🔴 vermelho | 🟢 verde | S | ✅ sim | Baseline acerta em não forçar zip/`config.properties` do SIGO (respeita o pedido "só o .msi"), mas falha a assertion da trava obrigatória: não confirma explicitamente que o WiX está instalado antes de gerar o `.msi` — discute o conteúdo do `.wxs` (UpgradeCode, versionamento) mas não a pré-condição de ferramenta na máquina atual. Pós-skill acerta as 3 assertions de forma explícita: declara a trava "Ambiente Windows + WiX instalado (já é pré-requisito do script existente, mas confirmar na máquina atual)", e nomeia item a item o que NÃO importa por reflexo do SIGO (zip com retentativa, `config.properties`, `--main-class Launcher`, `--add-modules java.se,jdk.unsupported`), contrastando explicitamente com o costume real deste projeto. Caso discriminante. |
| 3 — greenfield, primeiro empacotamento em .exe | sintético | 🔴 vermelho | 🟢 verde | S | ✅ sim | Baseline evita corretamente os itens do SIGO (não usa `Launcher`, não monta zip/config modelo, propõe fluxo mínimo) mas erra o alvo da assertion 1: a "SUPOSIÇÃO" que declara é sobre ferramenta de build (Maven vs. Gradle) e nome de classe, não sobre a FORMA DO ENTREGÁVEL exigida (RI-04) — a assertion específica não é atendida. Pós-skill declara a suposição no lugar certo ("SUPOSIÇÃO (RI-04)... entregável = pasta `dist/<App>/` solta"), monta checklist negativo completo (sem zip 5x, sem `config.properties`, sem `--main-class Launcher`, sem `--add-modules` sem confirmar necessidade) e propõe o app-image mínimo em vez do fluxo de 4 passos do SIGO — batendo as 5 assertions. Caso discriminante. |

## Leitura honesta do resultado

Nos 3 casos a skill discrimina na direção certa, mas por um padrão de falha específico e recorrente na baseline: não é que a baseline erre o empacotamento Java em si (comandos jpackage/WiX genéricos aparecem corretos nos três), é que ela erra a fronteira entre "costume real do projeto-alvo" e "reflexo do padrão SIGO" — no caso 1 ela inventa detalhes de plugin sem citar fonte (viola RO-01), nos casos 2 e 3 ela evita bem os excessos do SIGO por instinto de escopo enxuto, mas falha assertions específicas de disciplina (confirmar WiX instalado; declarar suposição no ponto certo — forma do entregável, não ferramenta de build). A skill, nos três, força a leitura da fonte real antes de agir (caso 1), transforma trava obrigatória em checklist explícito verificável (caso 2) e realoca a declaração de suposição para o RI-04 correto com checklist negativo nomeado item a item (caso 3). Amostra pequena (3 casos, sendo 1 real e 2 sintéticos) e o "acionou" é inferência por eliminação de um eval de disparo por description, não observação direta desta skill — dentro dessa limitação, o padrão é consistente: sem a skill, a baseline aproxima-se do comportamento certo mas tropeça em pontos de disciplina específicos (fonte real, trava explícita, foco da suposição); com a skill, esses pontos fecham.
