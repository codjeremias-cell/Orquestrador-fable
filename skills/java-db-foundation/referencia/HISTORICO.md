# Histórico — java-db-foundation

Registro de rodadas de evolução da skill (movido do corpo do SKILL.md para progressive disclosure; conteúdo verbatim).

- **2026-07-19 — Rodada 2 (destravamento, decisões D-A..D-D do Jeremias):** description descongelada (D-D) — removida a promessa fixa de "utilitário de retry para operações críticas (RetryDB)" (o corpo já manda espelhar a forma real, embutida ou separada); frases-gatilho preservadas. Duas citações soltas corrigidas: `referencia-exemplos-reais-sigo.md` tinha "desvio se declara (RI-01)" sobrevivente da correção anterior (ficou pela metade) → `(RO-01)`; bullet "Origem do caminho/URL do banco" tinha `declarar **SUPOSIÇÃO:**` sem cite → ganhou `(RO-01)`, alinhado às outras duas ocorrências. Citação de RO-10 (`try-with-resources no consumo`) realinhada ao texto vigente de REGRAS-DE-OURO.md (reescrito hoje mesmo, rastreio `_evolucao-skills/rodadas/R-lote1-fundacao-2026-07-19.md`): trocado "documentar no cabeçalho da classe" (extrapolação sem lastro na regra) por "try-with-resources obrigatório em `Connection`/`Statement`/`ResultSet`", texto literal da RO-10 nova. `evals/evals.json` (D-A): todos os 3 casos ganharam `origem` (`real`/`sintetico`) + `fonte` + `data` + `sessao`; Caso 1 (SIGO) = `real`, fonte `referencia-exemplos-reais-sigo.md`; Caso 2 ("outra casa" HikariCP+`ConexaoRetry.executar`) investigado contra as referências reais do Gradup (`springboot-repository-service`, `springboot-entity`, `springboot-controller-thymeleaf`) — nenhuma cobre fundação de banco/DataSource/retry (cobrem repositório JPA, entidade+migração e controller/Thymeleaf); mantido `sintetico` com a lacuna declarada (RO-01), não promovido a `real` por falta de fonte citável; Caso 3 (greenfield) = `sintetico`, sem projeto real por trás (declarado no próprio enunciado). JSON validado por parser Python real (`json.load`), 3 casos, todos com `origem`.
- **2026-07-19 — Correção pós-Painel 1 (citações e duplicação):** "Segredos fora do git" citava `(RO-04)` (é anti-injection — regra errada; o bullet real é o item não numerado de "Padrões universais" em REGRAS-DE-OURO.md); as duas instâncias de "declare **SUPOSIÇÃO:**" citavam `(RI-01)`, corrigidas para `(RO-01)` (convenção confirmada em desktop-tauri-scaffold, desktop-packaging, web-data-layer e outras). Parágrafo "Gabarito SIGO" enxugado — a mecânica completa (retry 3×, trava em 2 níveis, `config.properties`) já vive em `referencia-exemplos-reais-sigo.md`; o corpo mantém só o fato diferenciador (sem `RetryDB` separado) + ponteiro. Adicionada 1 linha reconhecendo a divergência deliberada do template §5b (invariante × variável em vez de "Convenções obrigatórias"/"Regras de implementação"). Não corrigidos (ver rodada de juízes): gap de `origem: real|sintetico` no eval (sistêmico no catálogo, fora do escopo desta skill isolada) e lastro de código real para o ramo "outra casa" (HikariCP) da referência (exigiria inventar fonte sem projeto real por trás — RO-01).
- **2026-07-19 — Evolução ao 9,5 (receita espelhar-não-impor, núcleo SIGO):** corpo reestruturado em **invariantes inegociáveis** (segredos fora do git, provedor único, try-with-resources, RO-08/RO-11, sem corrupção por concorrência) × **o que varia por projeto** (onde vive o retry — classe separada `RetryDB` ou embutido no provedor; mecanismo de serialização; política de retry; origem do caminho do banco). Removida a fricção "corpo manda criar um utilitário `RetryDB` à parte × a referência SIGO real embute retry e a trava em 2 níveis dentro da própria `Database`" — o Fluxo agora detecta a forma real (passo 2) antes de resolver a resiliência (passo 4). Verificação de fechamento (RI-04) tornada condicional (SKIP declarado sem banco acessível, espelhando que o SIGO não tem teste automatizado do provedor). Eval em `evals/evals.json` (3 casos: SIGO → retry embutido na `Database`, sem classe `RetryDB` separada; outra casa com pool + utilitário próprio → espelha essa casa mantendo os invariantes; greenfield → decisão simples marcada `SUPOSIÇÃO:`, sem importar o padrão SIGO).
- **2026-07-18 — Few-shot de código real (2ª leva Java, código do SIGO):** criada `referencia-exemplos-reais-sigo.md` (fonte: código vivo do SIGO/sistema-cot). Baseline §11: sem o gabarito o gerador diverge nos pontos-chave da casa (ver referência). Degrau §6.10: 2 (referência nova).
- **2026-07-13 — Poda de duplicação P1 (auditoria de notas das 52 skills):** fonte única + referência com gloss (PADRAO §12.5); item E9; −1 linha.
